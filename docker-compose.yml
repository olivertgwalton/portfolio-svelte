services:
  portfolio:
    image: docker.io/library/portfolio
    restart: unless-stopped
    ports:
      - "3001:3000"
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
